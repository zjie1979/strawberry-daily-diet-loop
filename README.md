# 草莓饮食-平常的饮食

公开静态 PWA。用于在 iPhone Safari 打开并添加到主屏幕。

- 三页式结构：首页打卡、餐单库、记录统计。
- 数据只保存在当前浏览器的 localStorage。
- 每天从餐单库里自己选定一份餐单打卡，不自动循环。
- 餐单库按系列进入，再查看每天可选餐单和详情。
- 记录页显示每份餐单累计打卡次数。
- 不包含账号、服务端、私密数据或第三方接口。
